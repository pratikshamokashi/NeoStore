<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.neostoreapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ui.base.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.myorder.MyOderListActivity"></activity>
        <activity android:name=".ui.address.AddressListingActivity" />
        <activity android:name=".ui.address.AddressDataActivity" />
        <activity android:name=".ui.mycartlisting.MyCartActivity" />
        <activity android:name=".ui.productdetails.ProductDetailActivity" />
        <activity android:name=".ui.editprofile.EditProfileActivity" />
        <activity android:name=".ui.accountdetails.MyAccountActivity" />
        <activity android:name=".ui.resetpassword.ResetPasswordActivity" />
        <activity android:name=".ui.product.ProductActivity" />
        <activity android:name=".ui.productlisting.ProductActivity" />
        <activity
            android:name=".Navigation"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.base.BaseActivity" />
        <activity android:name=".ui.registration.RegisterActivity" />
        <activity android:name=".ui.resetpassword.Reset1PasswordActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>